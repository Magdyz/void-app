plugins {
    id("void.block")
}

android {
    namespace = "app.voidapp.block.identity"
}

// Block-specific dependencies (in addition to what void.block provides)
dependencies {
    // None needed - this block only uses slate/core and slate/design
}
